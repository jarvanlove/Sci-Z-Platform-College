spring:
  application:
    name: sci-z-server
  profiles:
    active: dev

logging:
  config: classpath:logback-spring.xml

management:
  endpoints:
    web:
      exposure:
        include: health,info
