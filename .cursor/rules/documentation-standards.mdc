---
description: "文档编写标准：Markdown 规范、结构、渐进式编写、元数据与专业输出"
alwaysApply: true
globs: ["**/*.md", "**/*.mdx", "**/README*", "**/doc/**"]
---

# 文档编写标准

本规则在编写或修改 Markdown/MDX 文档、README、doc 目录下内容时生效；也可通过 @ 引用本规则主动应用。

---

## 1. 文档元数据（必选，保证输出专业）

在文档开头必须包含以下元信息（可用 YAML front matter 或首段表格）：

| 字段         | 说明                     | 示例                          |
| ------------ | ------------------------ | ----------------------------- |
| **编写时间** | 文档创建或重大修订的日期 | 2026-02-03 或 2026-02-03 修订 |
| **维护人**   | 负责维护该文档的人或团队 | 张三 / 平台组                 |

**YAML 示例（推荐）：**

```yaml
---
title: 文档标题
编写时间: 2026-02-03
维护人: Jiawen.Wu
---
```

**无 front matter 时**：在正文第一段用表格或列表写明「编写时间」「维护人」，再开始正文。

---

## 2. 渐进式编写模式（防止超时与丢失）

当待输出内容**较长**（例如单文件超过约 500 行、或预估生成时间较长易超时）时，**必须**采用渐进式编写，避免一次性生成导致超时或失败后全部丢失。

### 2.1 适用场景

- 单次需生成或重写超过约 500 行的文档
- 单次需生成超过约 300 行的代码文件中的注释/文档部分
- 用户明确要求「分段写」「边写边保存」或「防止超时」

### 2.2 执行要求

1. **先规划结构**：列出目录/章节或逻辑单元，确定写入顺序。
2. **按单元写入**：每完成一个逻辑单元（如一章、一节、一个完整示例块）就**立即写入文件**，不等到全部内容生成完毕再一次性写入。
3. **单次写入量**：每批写入建议控制在约 200 ～ 300 行以内；若单章超过该量，可再拆成多段写入。
4. **新文件**：先写入头部（元数据、目录、第一章标题），再逐章/节用追加或替换方式补全，确保每次写入后文件可读、结构完整。
5. **进度说明**：每完成一批写入，可简要说明「已完成 xxx 部分，正在写入 xxx」，便于用户确认和续写。
6. **失败处理**：若某次写入失败，记录失败位置并告知用户，便于从该处恢复，不假定之前未保存的内容仍存在。

### 2.3 禁止做法

- 在长文档场景下，等所有内容在内存中生成完毕再一次性写入。
- 单次生成超过约 1000 行而不做分段写入。
- 忽略写入错误仍继续生成后续内容而不提示用户。

---

## 3. Markdown 规范

- 使用清晰的标题层级（如一级标题文档标题，二级章节，三级小节），层级不超过 4 级为佳。
- 代码块必须指定语言类型（如 ` ```javascript `、` ```bash `）。
- 结构化数据用表格展示；多列时表头与单元格对齐。
- 复杂流程、状态机、依赖关系使用 Mermaid 图表（flowchart、sequenceDiagram、stateDiagram 等）。

---

## 4. 文档结构

- **必须有目录**：超过一屏的文档在开头提供目录（可自动生成或手写），便于跳转。
- **章节编号**：长文档建议使用清晰的章节编号（如 1、1.1、1.2）。
- **示例代码**：需要说明用法或配置的地方，包含可运行的示例代码或命令，并注明语言类型。
- **参考链接**：引用外部规范、API、教程时提供可点击的 URL；内部文档用相对路径。

---

## 5. 修订时的元数据更新

对文档做**实质性修订**时，应更新「编写时间」为本次修订日期，并视情况更新「维护人」；可在文末增加简短修订记录（日期 + 修订要点）。
