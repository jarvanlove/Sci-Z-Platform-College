---
description: "通用编码范式与最佳实践，适用于前后端"
alwaysApply: true
---

# 通用编码范式

## 代码质量与结构

- **可读性**：代码即文档，优先通过命名与结构表达意图；避免晦涩缩写与魔法数字，必要时用常量/枚举命名。
- **单一职责**：函数/方法/组件只做一件事；类/模块聚焦一个变更原因；过长函数（如 >50 行）考虑拆分为更小单元。
- **DRY**：重复逻辑抽取为函数、工具模块或共享组件；相似模式出现 ≥2 次即考虑抽象，但不过度抽象（YAGNI）。
- **可维护与可扩展**：依赖抽象/接口而非具体实现；通过配置或策略扩展行为，避免为每种情况写死分支；公共 API 保持稳定，内部实现可演进。

## 命名规范

- **变量/函数/方法**：使用完整、有业务含义的名称；布尔用 `is/has/can` 等前缀；避免单字母（除循环下标、数学符号等约定俗成）。
- **类/组件/模块**：名词或名词短语，体现职责；文件名与主要导出名一致（按语言惯例）。
- **常量**：全大写下划线或 PascalCase（按语言惯例），语义明确。

## 函数与方法

- 参数不宜过多（建议 ≤5）；过多时用对象/配置参数或拆分为更小函数。
- 避免深层嵌套（如 >3 层 if/for）；早返回（early return）或卫语句减少嵌套。
- 有副作用的操作（IO、网络、改全局状态）与纯计算分离，便于测试与推理。

## 错误与边界

- 显式处理错误与异常，不吞掉异常；对外接口用统一错误形态（如错误码、Result 类型、约定格式的异常）。
- 校验输入与边界条件；对无效输入快速失败并给出清晰反馈。

## 注释与文档

- **公共 API**：必须有文档注释（用途、参数、返回值、异常/错误、示例若有必要）。
- **复杂逻辑**：非自解释的算法、业务规则或非显然的“为什么这样写”须有简短注释说明意图。
- 优先通过命名与结构减少对注释的依赖；注释与代码同步更新，过时即删。
