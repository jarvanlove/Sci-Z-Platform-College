# 沐曦 C500/C550 64GB 单卡大模型推理

快速估算 & 推荐方案指南

## 一句话核心卖点（建议背下来）

单张 64GB 沐曦卡最推荐的配置：  
AWQ 4bit 量化的 70B~72B 级别模型  
可稳定支持 5~12 人同时提问（8k 上下文左右），  
国产可控、性价比高，是目前企业最常选择的推理方案。

## 1. 推荐量化方式详细介绍（给客户讲解用）

| 量化方式     | 每参数比特数 | 显存占用（相对 FP16） | 推理速度提升 | 质量损失（常见情况）  | 推荐指数 | 通俗解释（销售对客户说）         |
| ------------ | ------------ | --------------------- | ------------ | --------------------- | -------- | -------------------------------- |
| FP16 / BF16  | 16 bit       | 100%（基准）          | 基准         | 无损失                | ★★★☆☆    | 原版精度最高，但最占显存         |
| INT8 / W8A16 | 8 bit 权重   | ≈50%                  | 快 30~80%    | 极小（基本看不出来）  | ★★★★☆    | 基本无损压缩，大部分客户接受     |
| GPTQ 4bit    | 4 bit 权重   | ≈28~32%               | 更快         | 小到中等（3~8%下降）  | ★★★★☆    | 最早的 4bit 方案，社区最成熟     |
| AWQ 4bit     | 4 bit 权重   | ≈28~32%               | 最快         | 极小（通常 1~4%下降） | ★★★★★    | 目前精度+速度最佳平衡，首推      |
| 3bit / 2bit  | 3bit / 2bit  | <25%                  | 更快         | 中等到很大            | ★★☆☆☆    | 显存极度紧张才考虑，质量明显下降 |

重点话术：  
“现在我们最推荐用 AWQ 4bit 量化，70B 模型显存占用只有原版的约 1/3.5，速度明显更快，回答质量损失通常只有 1~4%，绝大部分业务场景人类几乎感觉不到差别，是目前性价比最高的国产 GPU 部署方式。”

## 2. KV Cache 是什么？有什么作用？为什么这么吃显存？

最通俗的比喻（直接讲给客户听）：  
想象你在跟一个超级聪明的 AI 聊天，但它有“短期记忆障碍”：

- 没有 KV Cache：每次你说一句话，它都要把前面整段对话从头重新算一遍 → 特别慢
- 有了 KV Cache：相当于给 AI 一个“对话记事本”，把之前聊过的关键点（Key=线索，Value=内容）都记下来
  下次你再说话，它只需要翻记事本 + 看你最新一句话，就能立刻接上

两大核心作用：

1. 速度提升几十倍甚至上百倍（生成新 token 只算最新部分）
2. 但代价是：这个“记事本”会越写越长，同时聊的人越多，记事本就越大 → 非常吃显存

显存占用快速口诀（70B 级别模型最常用）：

- 每个同时提问的用户，每 1000 个上下文长度（输入+输出总和） ≈ 占用 2.0~2.5 GB 显存

举例：
8 个人同时提问，每人平均对话 8000 个 token  
→ KV Cache 大约占用 8 × 8 × 2.2 GB ≈ 140 GB  
→ 单卡 64GB 肯定不够，至少需要 3 张卡（实际优化后可能 2.5~3 张）

一句话总结给客户：  
“KV Cache 让模型回答速度快很多，但它就像聊天记录，越多人同时聊、每人聊得越长，占用的显存就越多，所以我们需要根据你们的实际使用场景来平衡模型大小和能支持的人数。”

## 3. 客户需求收集与推荐逻辑（两种场景）

### 场景 A：客户清楚自己需求（比较少见）

直接问以下 5 个问题：

1. 想部署的主要模型大概多少参数？（7B / 13B / 34B / 70B / 110B+）
2. 最多希望多少人同时提问？（峰值并发用户数）
3. 一次典型对话的上下文长度大概多少？（输入+输出 token 总和）
   - 常见：4000~8000 token
   - 中等：16000~32000 token
   - 很长：>64000 token
4. 对回答质量要求高吗？能不能接受 4bit 量化？
5. 是否有很多长输出需求？（写长文、长代码、报告等）

然后参考下表快速匹配：

| 模型规模 | 最推荐量化   | 权重占用约                               | 建议预留 KV Cache 空间 | 单卡能支持最大并发（参考）          | 推荐卡数建议          |
| -------- | ------------ | ---------------------------------------- | ---------------------- | ----------------------------------- | --------------------- |
| ≤13B     | FP16 或 INT8 | 16~28GB                                  | 25~35GB                | 20~50 人 @4k~8k                     | 1 张                  |
| 30~34B   | INT8 或 AWQ4 | 30~42GB                                  | 18~25GB                | 10~25 人 @4k 6~15 人 @8k            | 1~2 张                |
| 70~72B   | AWQ 4bit     | 38~44GB                                  | 15~22GB                | 8~16 人 @4k 5~10 人 @8k 2~5 人 @32k | 1~4 张（甜点 2~3 张） |
| 110~123B | AWQ 4bit     | 60~72GB                                  | 很少余量               | 1~4 人 @短上下文                    | 至少 2~4 张           |
| >130B    | 4bit 或更低  | >75G# 沐曦 C500/C550 64GB 单卡大模型推理 |

快速选型 & 方案推荐指南  
（销售 + 技术共用 · 含上下文 token 形象解释）

## 一句话核心卖点

单张 64GB 沐曦卡最推荐配置：  
AWQ 4bit 量化的 70B~72B 模型  
可稳定支持 5~12 人并发（典型 4k~8k 上下文），  
国产可控、性价比高，是当前企业级推理最主流落地方案。

## 1. 量化方式对比（技术细节 + 销售话术）

| 量化方式          | 权重显存占比（相对 FP16） | 推理速度 | 质量损失（典型 70B 模型） | 推荐指数 | 销售对客户话术要点                 |
| ----------------- | ------------------------- | -------- | ------------------------- | -------- | ---------------------------------- |
| FP16 / BF16       | 100%（基准，70B ≈140GB）  | 基准     | 无损失                    | ★★★☆☆    | 原版精度最高，但单卡基本跑不了 70B |
| INT8 / W8A16      | ≈50%（70B ≈70–75GB）      | +30–80%  | 极小（<2%）               | ★★★★☆    | 基本无损，精度敏感客户可选         |
| GPTQ 4bit         | ≈28–33%（70B ≈39–46GB）   | 更快     | 小到中等（3–8%）          | ★★★★☆    | 社区最早 4bit，稳定但非最优        |
| AWQ 4bit          | ≈27–32%（70B ≈38–45GB）   | 最快     | 极小（通常 1–4%）         | ★★★★★    | 目前精度 × 速度最佳，强烈首推      |
| 更低（3bit/2bit） | <25%                      | 更快     | 中等到严重                | ★★☆☆☆    | 显存极紧时应急，不建议常规业务     |

**重点结论**：AWQ 4bit 是当前 70B 级别模型的最佳平衡点，权重占用约原版 FP16 的 1/3.2 ~ 1/3.7（文档简化说“约 1/3.5”），实际加载 38–45GB，留足空间给 KV Cache 和并发。

## 2. KV Cache 是什么？为什么这么吃显存？（通俗 + 技术版）

**通俗比喻**（讲给客户/销售）：  
KV Cache = AI 的“对话记事本”。没有它，每次回答都要重读整段历史 → 极慢；有了它，只需看最新一句 + 翻记事本 → 速度提升几十倍。但记事本会越写越厚，同时聊的人越多、每人聊得越久，显存占用就线性暴涨。

**技术本质**：  
KV Cache 存储每层注意力机制的 Key 和 Value 向量。  
大小公式（简化）：  
KV Cache (GB) ≈ batch_size × num_layers × 2 × seq_len × hidden_size × bytes_per_kv_element / 1e9

**参数含义说明**：

- **batch_size**：批次大小，即同时处理的请求数量（并发用户数）。例如：8 个用户同时提问，batch_size = 8
- **num_layers**：Transformer 模型的层数（注意力层数量）。例如：Llama3.1-70B 有 80 层，Qwen2.5-72B 有 80 层
- **2**：固定系数，表示 Key 和 Value 两个向量（每个 token 需要存储 K 和 V 两个向量）
- **seq_len**：序列长度，即上下文长度（输入 + 输出 token 总数）。例如：8000 token、16000 token
- **hidden_size**：隐藏层维度，模型每层的特征向量维度。例如：70B 模型通常为 8192 或 10240
- **bytes_per_kv_element**：每个 KV 元素的字节数。FP16 为 2 字节，INT8 为 1 字节，INT4 为 0.5 字节
- **1e9**：单位转换系数，将字节转换为 GB（1 GB = 10^9 字节）

**快速记忆**：KV Cache 大小 = 并发数 × 层数 × 2（K+V）× 上下文长度 × 特征维度 × 数据类型字节数

70B 模型典型值（FP16 KV）：

- 每 1k token、每 1 个并发 ≈ 2.0–2.5 GB（视 hidden_size 8192~8192+）
- 8 人并发 × 8k 上下文 ≈ 128–160 GB → 单卡 64GB 放不下，必须多卡或量化 KV

**优化方向**（技术可讨论）：

- KV Cache INT8/INT4 量化 → 节省 50%–75%
- PagedAttention / vLLM 连续批处理 → 降低峰值浪费
- 限制 max_context 或 rolling buffer → 控制上限

## 3. 上下文长度（token）形象对照表（最实用参考）

| 英文（≈0.75 词/token） | 中文（≈1.7–1.9 字符/token） | 典型业务场景举例               |
| ---------------------- | --------------------------- | ------------------------------ |
| 500–800 tokens         | 250–400 汉字                | 短微信/即时客服单轮            |
| 1000 tokens            | 500–600 汉字                | 中等邮件 / 简单问答            |
| 2000 tokens            | 1000–1200 汉字              | 短公众号文 / 产品需求初稿      |
| 4000 tokens            | 2000–2500 汉字              | 标准技术博客 / 完整客服会话    |
| 8000 tokens            | 4000–5000 汉字              | 深度报告 / 长会议纪要          |
| 16000 tokens           | 8000–10000 汉字             | 完整 PRD / 中篇论文            |
| 32000 tokens           | 15000–20000 汉字            | 长合同 / 小说章节 / 多文档 RAG |

**最常见企业场景**：  
4k–8k token（输入+输出总和）已覆盖 80%+ 业务（客服、知识库、代码辅助、文案）

## 4. 客户需求收集与推荐逻辑

### A. 客户清楚需求（少见）

问清：模型参数、峰值并发、典型上下文（token 或字数）、是否接受 4bit、长输出比例  
→ 直接查第 5 节表格匹配卡数

### B. 客户不清楚需求（最常见，默认方案）

销售标准话术：

“现在 90% 企业最终落地都是 70B 级别模型 + AWQ 4bit 量化。  
在一张 64GB 沐曦卡上，通常可支持 5–10 人同时提问（每人对话 4000–8000 token，约 2000–5000 汉字），覆盖智能客服、知识问答、代码辅助、文案生成等主流场景。

优点：

- 质量接近原版 70B（损失通常 <4%）
- 速度快（沐曦硬件 + 4bit 友好）
- 初期投资低（1–2 张卡即可）
- 扩展性好（业务增长直接加卡）

建议：**先从 2 张卡起步**，上线跑 1–2 个月观察真实并发和上下文消耗，再决定是否加卡 / 调整模型。这是最稳妥、低风险的路径。”

**默认推荐配置**（客户模糊时）：

- 模型：70B~72B（Llama3.1-70B / Qwen2.5-72B 等）
- 量化：AWQ 4bit
- 卡数：2–3 张（初期 2 张，预留扩展）
- 预期：5–12 人并发，4k–8k 上下文

## 5. 单卡 64GB 快速估算表（核心参考）

| 模型规模 | 推荐量化  | 权重占用（约） | 建议预留 KV+其他 | 单卡最大并发参考（FP16 KV）         |
| -------- | --------- | -------------- | ---------------- | ----------------------------------- |
| ≤13B     | FP16/INT8 | 16–28GB        | 25–35GB          | 20–50 人 @4k–8k                     |
| 30–34B   | INT8/AWQ4 | 30–42GB        | 18–25GB          | 10–25 人 @4k 6–15 人 @8k            |
| 70–72B   | AWQ 4bit  | 38–45GB        | 15–22GB          | 8–16 人 @4k 5–10 人 @8k 2–5 人 @32k |
| 110–123B | AWQ 4bit  | 60–72GB        | 很少             | 1–4 人 @短上下文                    |
| >130B    | 4bit+     | >75GB          | —                | 单卡不可行，多卡必选                |

### 场景 B：客户不清楚需求（最常见情况）

绝大部分客户会说：“我们也不知道具体要多大模型”“还没统计过同时多少人”“上下文长度不清楚”

此时推荐统一话术 + 标准化方案：

标准开口话术（建议直接复读）：

“现在市场上 90% 以上的企业客户，最终落地使用的都是 70B 级别的模型，用 AWQ 4bit 量化方式部署。  
这种配置在一张 64GB 的沐曦卡上，通常可以稳定支持 5~10 个人左右同时提问（每人对话长度在 4000~8000 个 token 左右），已经能覆盖绝大多数业务场景，比如智能客服、内部知识库问答、代码辅助、文案生成等。

优点总结：

- 回答质量高（接近原版 70B 水平，差别很小）
- 速度快（4bit 量化 + 沐曦硬件优化）
- 初期投资低（1~2 张卡就能跑起来）
- 后期扩展方便（业务增长了直接加卡）

如果你们现在还没有特别明确的并发和上下文数据，我们建议先从 2 张 64GB 沐曦卡起步，用 70B AWQ 4bit 模型跑起来，实际使用 1~2 个月后观察真实负载，再决定是否加卡或调整模型大小。这样风险最低，扩展性也最好。”

标准化推荐配置（客户模糊需求时的默认方案）：

目标模型：70B~72B 级别（Llama3.1-70B、Qwen2.5-72B 等）  
量化方式：AWQ 4bit（首选）  
推荐卡数：2~3 张（初期 2 张，留扩展空间）  
预期能力：5~12 人同时在线提问，平均上下文 4k~8k  
后续优化空间：可开启 KV Cache INT8/INT4 进一步提升 20~40% 并发
