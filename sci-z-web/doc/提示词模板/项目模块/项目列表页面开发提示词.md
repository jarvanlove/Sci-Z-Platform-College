# 项目列表页面开发提示词

## 📋 页面信息
- **原型图路径**：`原型图/项目模块/项目列表.html`
- **页面类型**：列表页面
- **页面名称**：项目管理
- **主要功能**：项目搜索、列表展示、分页、增删改查
- **对应文件路径**：`src/views/Project/List.vue`

## 🎨 原型图视觉分析

### **整体布局**
- 背景色：`#f7f9fc`
- 容器：`padding: 20px`，`min-height: calc(100vh - 56px)`
- 页面标题：`font-size: 24px`，`color: #1e3a8a`，`font-weight: 600`

### **搜索区域设计**
- 白色卡片：`background: #ffffff`，`border-radius: 8px`，`padding: 20px`
- 阴影效果：`box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04)`
- 边框：`border: 1px solid #e5e7eb`
- 表单布局：水平排列，`gap: 16px`，`align-items: end`

### **表格设计**
- **使用Element Plus表格组件**：`el-table`、`el-table-column`
- 表头：`background-color: #f8fafc`，`color: #6b7280`，`font-weight: 600`
- 行悬停：`background-color: #f0f9ff`
- 奇偶行：偶数行`background-color: #f9fafb`
- 进度条：使用`el-progress`组件，不同进度不同颜色

### **状态标签设计**
- 圆角标签：`border-radius: 12px`，`padding: 4px 8px`
- 不同状态不同颜色：
  - 进行中：`#fef3c7`背景，`#f59e0b`文字
  - 已完成：`#dcfce7`背景，`#16a34a`文字
  - 已延期：`#fee2e2`背景，`#dc2626`文字

## 🔧 技术实现要求

### 1. 页面结构
```vue
<template>
  <div class="project-list-container">
    <!-- 页面标题 -->
    <div class="page-header">
      <h1 class="page-title">{{ $t('project.list.title') }}</h1>
      <!-- 注意：原型图中新建项目按钮被注释了，因为项目由申报成功后后台直接生成 -->
    </div>

    <!-- 搜索筛选区域 -->
    <div class="search-card">
      <el-form :model="searchForm" class="search-form" label-width="100px">
        <el-form-item :label="$t('project.list.keywordSearch')">
          <el-input
            v-model="searchForm.keyword"
            :placeholder="$t('project.list.keywordPlaceholder')"
            clearable
            style="width: 300px"
            @keyup.enter="handleSearch"
          >
            <template #prefix>
              <el-icon><Search /></el-icon>
            </template>
          </el-input>
        </el-form-item>
        <el-form-item :label="$t('project.list.statusFilter')">
          <el-select
            v-model="searchForm.status"
            :placeholder="$t('project.list.statusPlaceholder')"
            clearable
            style="width: 150px"
            @change="handleStatusChange"
          >
            <el-option
              v-for="option in statusOptions"
              :key="option.value"
              :label="option.label"
              :value="option.value"
            />
          </el-select>
        </el-form-item>
        <el-form-item :label="$t('project.list.dateFilter')">
          <el-date-picker
            v-model="searchForm.dateRange"
            type="daterange"
            range-separator="至"
            :start-placeholder="$t('project.list.startDate')"
            :end-placeholder="$t('project.list.endDate')"
            style="width: 240px"
            format="YYYY-MM-DD"
            value-format="YYYY-MM-DD"
            @change="handleDateChange"
          />
        </el-form-item>
      </el-form>

      <!-- 按钮行 -->
      <div class="search-actions">
        <el-button type="primary" @click="handleSearch">
          <el-icon><Search /></el-icon>
          {{ $t('common.search') }}
        </el-button>
        <el-button @click="handleReset">
          <el-icon><Refresh /></el-icon>
          {{ $t('common.reset') }}
        </el-button>
      </div>
    </div>

    <!-- 项目列表表格 -->
    <div class="table-card">
      <!-- 加载状态 -->
      <div v-if="loading" class="loading-container">
        <el-icon class="is-loading"><Loading /></el-icon>
        <span>{{ $t('common.loading') }}</span>
      </div>

      <!-- Element Plus表格 -->
      <el-table
        v-else
        :data="projects"
        stripe
        style="width: 100%"
        :header-cell-style="{ background: '#f8fafc', color: '#6b7280', fontWeight: '600' }"
        :row-style="{ '&:hover': { backgroundColor: '#f0f9ff' } }"
      >
        <el-table-column prop="number" :label="$t('project.list.columns.number')" width="120" align="center" />
        <el-table-column prop="name" :label="$t('project.list.columns.name')" min-width="200" show-overflow-tooltip />
        <el-table-column prop="manager" :label="$t('project.list.columns.manager')" width="120" align="center" />
        <el-table-column prop="status" :label="$t('project.list.columns.status')" width="100" align="center">
          <template #default="{ row }">
            <el-tag
              :type="getStatusTagType(row.status)"
              size="small"
              round
            >
              {{ getStatusText(row.status) }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="progress" :label="$t('project.list.columns.progress')" width="120" align="center">
          <template #default="{ row }">
            <el-progress
              :percentage="row.progress"
              :color="getProgressColor(row.progress)"
              :stroke-width="6"
              :show-text="false"
            />
            <span class="progress-text">{{ row.progress }}%</span>
          </template>
        </el-table-column>
        <el-table-column prop="startTime" :label="$t('project.list.columns.startTime')" width="140" align="center" />
        <el-table-column prop="endTime" :label="$t('project.list.columns.endTime')" width="140" align="center" />
        <el-table-column :label="$t('common.actions')" width="180" align="center" fixed="right">
          <template #default="{ row }">
            <div class="operation-buttons">
              <el-button size="small" type="success" link @click="handleProgress(row)">
                {{ $t('project.list.progress') }}
              </el-button>
              <el-button size="small" type="primary" link @click="handleView(row)">
                {{ $t('common.view') }}
              </el-button>
              <el-button size="small" link @click="handleEdit(row)">
                {{ $t('common.edit') }}
              </el-button>
              <el-button
                v-if="row.canDelete"
                size="small"
                type="danger"
                link
                @click="handleDelete(row)"
              >
                {{ $t('common.delete') }}
              </el-button>
            </div>
          </template>
        </el-table-column>
      </el-table>

      <!-- 分页 -->
      <div class="pagination-container">
        <el-pagination
          v-model:current-page="currentPage"
          v-model:page-size="pageSize"
          :page-sizes="[10, 20, 50, 100]"
          :total="total"
          layout="total, sizes, prev, pager, next, jumper"
          @size-change="handleSizeChange"
          @current-change="handleCurrentChange"
        />
      </div>
    </div>
  </div>
</template>
```

### 2. 核心功能实现
```javascript
<script setup>
import { ref, reactive, onMounted, computed } from 'vue'
import { useRouter } from 'vue-router'
import { useI18n } from 'vue-i18n'
import { ElMessage, ElMessageBox } from 'element-plus'
import { Search, Refresh, Loading } from '@element-plus/icons-vue'
import { formatDate } from '@/utils/date'
import { PROJECT_STATUS, PROJECT_STATUS_CONFIG } from '@/utils/constants'
import { createLogger } from '@/utils/simpleLogger'

const router = useRouter()
const { t } = useI18n()
const logger = createLogger('ProjectList')

// 响应式数据
const loading = ref(false)
const projects = ref([])
const total = ref(0)
const currentPage = ref(1)
const pageSize = ref(10)

// 搜索表单
const searchForm = reactive({
  keyword: '',
  status: '',
  dateRange: []
})

// 状态选项（从constants.js导入的项目状态常量生成）
const statusOptions = ref([
  { label: t('common.all'), value: '' },
  { label: PROJECT_STATUS_CONFIG[PROJECT_STATUS.PROGRESS].text, value: PROJECT_STATUS.PROGRESS },
  { label: PROJECT_STATUS_CONFIG[PROJECT_STATUS.COMPLETED].text, value: PROJECT_STATUS.COMPLETED },
  { label: PROJECT_STATUS_CONFIG[PROJECT_STATUS.DELAYED].text, value: PROJECT_STATUS.DELAYED }
])

// 获取状态文本（使用constants.js的配置）
const getStatusText = (status) => {
  return PROJECT_STATUS_CONFIG[status]?.text || status
}

// 获取状态标签类型（使用constants.js的配置）
const getStatusTagType = (status) => {
  return PROJECT_STATUS_CONFIG[status]?.type || 'info'
}

// 获取进度条颜色
const getProgressColor = (progress) => {
  if (progress <= 30) return '#dc2626'
  if (progress <= 70) return '#f59e0b'
  return '#16a34a'
}

// 加载项目列表（通过接口获取）
const loadProjects = async () => {
  try {
    loading.value = true
    logger.info('Loading project list', {
      page: currentPage.value,
      size: pageSize.value,
      keyword: searchForm.keyword,
      status: searchForm.status,
      dateRange: searchForm.dateRange
    })

    // TODO: 替换为实际后端接口
    // const response = await getProjectList({
    //   page: currentPage.value,
    //   size: pageSize.value,
    //   keyword: searchForm.keyword,
    //   status: searchForm.status,
    //   startDate: searchForm.dateRange?.[0],
    //   endDate: searchForm.dateRange?.[1]
    // })
    // projects.value = response.data.list
    // total.value = response.data.total

    // 临时模拟数据（后端开发完成后删除）
    await new Promise(resolve => setTimeout(resolve, 800))
    const mock = [
      {
        id: 1,
        number: 'PRJ2024001',
        name: '人工智能在教育领域的应用研究',
        manager: '张教授',
        status: PROJECT_STATUS.PROGRESS,
        progress: 75,
        startTime: '2024-01-01',
        endTime: '2024-12-31'
      },
      {
        id: 2,
        number: 'PRJ2024002',
        name: '区块链技术在供应链管理中的应用',
        manager: '李教授',
        status: PROJECT_STATUS.COMPLETED,
        progress: 100,
        startTime: '2024-01-15',
        endTime: '2024-11-30'
      }
    ]
    projects.value = mock
    total.value = mock.length

    logger.info('Project list loaded', { count: projects.value.length })
  } catch (error) {
    logger.error('Failed to load project list', error)
    ElMessage.error(t('project.list.loadError'))
  } finally {
    loading.value = false
  }
}

// 搜索处理
const handleSearch = () => {
  currentPage.value = 1
  loadProjects()
}

// 重置搜索
const handleReset = () => {
  Object.assign(searchForm, {
    keyword: '',
    status: '',
    dateRange: []
  })
  currentPage.value = 1
  loadProjects()
}

// 状态变化
const handleStatusChange = () => {
  handleSearch()
}

// 日期变化
const handleDateChange = () => {
  handleSearch()
}

// 分页处理
const handleSizeChange = (size) => {
  pageSize.value = size
  currentPage.value = 1
  loadProjects()
}

const handleCurrentChange = (page) => {
  currentPage.value = page
  loadProjects()
}

// 操作处理
const handleView = (project) => {
  router.push(`/project/detail/${project.id}`)
}

const handleEdit = (project) => {
  router.push(`/project/detail/${project.id}?mode=edit`)
}

const handleProgress = (project) => {
  router.push(`/project/progress/${project.id}`)
}

const handleDelete = async (project) => {
  try {
    await ElMessageBox.confirm(
      t('project.list.deleteConfirm', { name: project.name }),
      t('project.list.deleteTitle'),
      {
        confirmButtonText: t('common.confirm'),
        cancelButtonText: t('common.cancel'),
        type: 'warning'
      }
    )
    
    // TODO: 替换为实际后端删除接口
    // await deleteProject(project.id)
    await new Promise(resolve => setTimeout(resolve, 500))
    
    ElMessage.success(t('common.deleteSuccess'))
    loadProjects()
  } catch (error) {
    if (error !== 'cancel') {
      ElMessage.error(t('common.deleteFailed'))
    }
  }
}

onMounted(() => {
  loadProjects()
})
</script>
```

### 3. 样式实现
```scss
<style lang="scss" scoped>
.project-list-container {
  padding: 20px;
  background: #f7f9fc;
  min-height: calc(100vh - 56px);
}

.page-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.page-title {
  font-size: 24px;
  font-weight: 600;
  color: #1e3a8a;
  margin: 0;
}

.search-card {
  background: #ffffff;
  border-radius: 8px;
  padding: 20px;
  margin-bottom: 20px;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04);
  border: 1px solid #e5e7eb;
}

.search-form {
  display: flex;
  gap: 16px;
  align-items: end;

  .el-form-item {
    margin-bottom: 0;
  }
}

.search-actions {
  display: flex;
  justify-content: flex-end;
  margin-top: 16px;
  gap: 8px;
}

.table-card {
  background: #ffffff;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04);
  border: 1px solid #e5e7eb;
}

.loading-container {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40px;
  gap: 8px;
  color: #6b7280;
}

// Element Plus表格样式
:deep(.el-table) {
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  overflow: hidden;
}

:deep(.el-table__header) {
  background-color: #f8fafc;
}

:deep(.el-table th) {
  background-color: #f8fafc !important;
  color: #6b7280 !important;
  font-weight: 600 !important;
  font-size: 13px !important;
}

:deep(.el-table td) {
  color: #374151;
}

:deep(.el-table--striped .el-table__body tr.el-table__row--striped td) {
  background-color: #f9fafb;
}

:deep(.el-table__body tr:hover > td) {
  background-color: #f0f9ff !important;
}

.progress-text {
  font-size: 12px;
  color: #6b7280;
  margin-left: 8px;
}

.operation-buttons {
  display: flex;
  gap: 8px;
  justify-content: center;

  .el-button {
    padding: 4px 8px;
    font-size: 12px;
  }
}

.pagination-container {
  display: flex;
  justify-content: center;
  margin-top: 20px;
}

// 响应式设计
@media (max-width: 768px) {
  .search-form {
    flex-direction: column;
    align-items: stretch;
    gap: 12px;
  }

  .search-actions {
    justify-content: center;
  }

  .native-table {
    font-size: 12px;
  }

  .col-number, .col-manager, .col-start-time, .col-end-time {
    width: 80px;
  }

  .col-name {
    width: 150px;
  }

  .operation-buttons {
    flex-direction: column;
    gap: 4px;
  }
}
</style>
```

## ⚠️ 重要注意事项

1. **严格按照原型图实现**：使用Element Plus表格组件（`el-table`、`el-table-column`）
2. **搜索功能**：关键词、状态、时间范围筛选
3. **进度条**：使用`el-progress`组件，不同进度不同颜色
4. **状态标签**：使用`el-tag`组件，圆角设计，不同状态不同颜色
5. **分页功能**：使用`el-pagination`组件，支持页码和每页条数调整
6. **操作按钮**：查看、编辑、删除、进度功能
7. **响应式设计**：移动端适配
8. **国际化支持**：所有文本使用$t()函数
9. **注意**：原型图中新建项目按钮被注释了，因为项目由申报成功后后台直接生成

**最终要求：生成的代码必须与原型图的视觉效果和交互逻辑完全一致！**