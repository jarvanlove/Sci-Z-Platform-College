---
description: "前端目录与职责、ToC 布局、开发流程、代码规范"
alwaysApply: false
globs: ["**/views/**", "**/components/**", "**/*.vue"]
---

# 前端架构落地规范

## 布局：ToC 为主

- **当前默认布局**：**ToC 布局**（`ToCLayout` + `ToCSidebar`）。左侧为产品主导航（新建对话、文献/知识库/实践、历史对话、用户与语言切换），右侧为主内容区；侧边栏可折叠。
- **布局选择**：在路由 `meta.layout` 中指定。未指定或 `meta.layout === 'toc'` 时使用 ToC；`meta.layout === 'main'` 时使用 `MainLayout`（管理型顶栏+侧栏，若需）；登录/注册/重置密码等无布局或 toc 下弹窗。
- **实现位置**：`src/App.vue` 根据 `getLayout(route)` 渲染 `ToCLayout` / `MainLayout`；布局组件在 `src/components/Layout/`（`ToCLayout.vue`、`ToCSidebar.vue`、`MainLayout.vue`、`Header.vue`、`Sidebar.vue`）。新增页面默认放在 ToC 布局内，无需改布局除非产品要求管理型页面。

## 目录与职责

- **views/**：仅路由挂载，极薄包装（引入业务组件或直接 `<router-view />` 所在布局内）。禁止在 view 中写复杂业务逻辑、接口调用或大量状态。
- **components/Business/<模块>/**：页面主体（布局块、表单、列表、数据、状态、样式）。新增功能按业务模块落在此目录，不新增额外目录层级。示例：`Dashboard/`、`Declaration/`、`Project/`、`Report/`、`User/`、`AI/`、`Knowledge/`、`System/`、`Auth/`；列表/详情/表单等可复用块在 `List/`、`Detail/`、`Form/`。
- **components/Common/**：通用组件（Base*、LanguageSwitcher、FilePreview 等），优先复用，见 `vue-components-and-upload.mdc`。
- **components/Layout/**：全局布局（ToCLayout、ToCSidebar、MainLayout、Header、Sidebar）。
- **api/<模块>/**：接口封装，按模块划分；**store/modules/**：Pinia；**utils/**：通用工具。业务组件从对应 `api/<模块>` 导入，禁止在 view 层直接调接口。

## 开发流程（具体步骤）

1. **阅读提示词模板**  
   在 `相关文档/提示词模板/` 下找到对应模块（申报、项目、验收、用户中心、仪表板、AI 助手等），确认交互、接口字段、校验规则与测试要点。

2. **实现业务组件**  
   - 在 `src/components/Business/<模块>/` 下新建或修改 `.vue` 文件（如 `DeclarationList.vue`）。  
   - 使用 `<script setup>` + Composition API；UI 用 Common 组件（BaseCard、BaseTable、BasePagination、BaseDialog 等）和 Element Plus。  
   - 接口：在 `api/<模块>/` 中定义方法（如 `api/Declaration/declaration.js`），业务组件内 `import { listDeclaration } from '@/api/Declaration/declaration'`，异步 `try/catch` + `ElMessage.error` 或日志，不静默失败。  
   - 状态：需要跨组件/持久化时用 Pinia（`useAuthStore`、`useIndustryStore` 等），其余用本地 `ref`/`reactive`。  
   - 跨模块数据在业务组件内处理，不在 view 层。

3. **视图包装**  
   - 在 `src/views/<模块>/` 下新建或修改页面（如 `List.vue`）。  
   - 内容仅为引入业务组件并渲染，例如：`<template><DeclarationList /></template>`，无逻辑可省略 `<script setup>` 或仅 `import` + `name`。

4. **配置路由**  
   - 在 `src/router/index.js` 中增加或修改路由，指定 `component` 指向上述 view；需 ToC 布局时设置 `meta: { layout: 'toc' }`（默认即为 toc）。  
   - 需要鉴权的路由设置 `meta: { requiresAuth: true }`。

5. **国际化**  
   - 所有文案使用 `$t('key')`。新增 key 时在 `src/locales/` 下同步维护 zh-CN、en-US、ja-JP、ko-KR 四个文件，键名一致。

6. **样式**  
   - 使用 `lang="scss" scoped`；间距等优先使用设计变量 `var(--gap-*)`、`var(--radius-*)`；响应式参考现有页面（断点、flex/grid）。

7. **验证与收尾**  
   - 本地验证：路由跳转、接口入参与响应、表单校验、i18n 键存在、不同视口下表现。  
   - 运行 `read_lints`（或 `npm run lint`）确保无新增警告。  
   - 总结中列出涉及的接口与主要 JSON 结构，便于联调与审查。

## 代码规范（具体）

- **日志**：使用 `createLogger('模块名')` 输出调试信息，禁止直接 `console.log` 提交；上线前清理无意义日志。
- **表单校验**：Element Plus 表单项配置 `rules`，通用规则可抽到 `utils/validate`；自定义校验失败时给出用户可理解的提示。
- **命名**：  
  - 文件名与组件名：PascalCase（如 `UserProfile.vue`、`DeclarationList.vue`）。  
  - 变量/方法：camelCase。  
  - 常量：大写下划线或与项目已有风格一致。  
  - 禁止在 `views/`、`components/Business/` 下新增一层与现有模块平级的目录（如不新增 `components/Business/NewModule/` 以外的层级）。
- **接口**：  
  - 先在 `api/<模块>/` 定义方法并 JSDoc 注释，再在业务组件中调用。  
  - 错误必须兜底：`ElMessage.error`（或等价的用户提示）并写日志，不静默失败；网络异常与业务错误码分开处理。
- **类型与可读性**：复杂数据建议 JSDoc 或 TypeScript 类型；单文件过长（如 >300 行）考虑拆成子组件或 composables。
